## 1. Project and source evidence

- [x] 1.1 Add `pyproject.toml` with the `simulation.actuator:OpenTorqueActuator` model and builder profile, create the importable package, and ignore build/snapshot artefacts while leaving every upstream file untouched. Initial root build: PASS (source-selected actuator housing).
- [x] 1.2 Add `simulation/tools/probe.py`; record merged STL connectivity, bounds, volumes, tooth-frequency evidence, STEP occurrence readings, source fingerprints, and catalogue citations in `docs/measurements.md`. Probe run: PASS on 2026-09-07.
- [x] 1.3 Generate the STEP product scaffold with `solid import-step`, reduce it to project-owned named `StepNode` selections with source-relative paths and material colours, and make the root render the first fixed shell subassembly. Finite root build: PASS.

## 2. Output stack

- [x] 2.1 Write output-stack contracts first for named STEP products, axial order, coaxiality, connected rigid bodies, and source fingerprint drift; run them faceted and retain the expected red evidence. Red: 3 failures for missing retainer/carrier/encoder products; fingerprint and housing connectivity passed.
- [x] 2.2 Implement the fixed housing/backplate/retainer/encoder-cover group, moving three-piece carrier and encoder-magnet group, and connected catalogue-dimension envelopes for compound or absent bought hardware until the output-stack contracts pass faceted. Green: 6/6 faceted contracts.
- [x] 2.3 Mutate one carrier axial placement beyond its measured seat margin, confirm the coaxial/seat contract fails, restore the source placement, and record the mutation result here. A +1 mm carrier-A Z mutation failed the axial-bounds contract with both endpoints displaced by 1 mm; restored run 6/6 green.

## 3. Planetary reducer

- [x] 3.1 Write reducer contracts first for the 18/54/126 tooth evidence, 27 mm planet radius, 120-degree spacing, home phase, pin/bearing coaxiality, and the 8:1/planet-spin relations; run them faceted and retain the expected red evidence. Red: 4 failures for absent planet units; tooth, kinematic, and sun connectivity baselines passed.
- [x] 3.2 Implement the sun input group and three orbiting planet units from the STEP-selected sun/planet geometry, bearings, and pins; bind all motion through ports and make the reducer contracts pass faceted. Green: 7/7 faceted contracts; full root finite build passes.
- [x] 3.3 Mutate the carrier ratio and one planet-center placement in node code, confirm the ratio and seating contracts fail respectively, restore both, and record any structural blind spot here. Dividing by 9 produced 40° instead of 45° and failed the independent ratio contract; a +1 mm planet-unit X shift failed the bore/bearing/pin seating contract. Restored run 7/7 green. The symmetric planet geometry cannot visually prove absolute spin after whole-tooth rotations, so the independent relation and serialized operation remain the spin evidence.

## 4. Actuator control and scenarios

- [x] 4.1 Write root contracts first for exactly one `input_angle` driver, all three instruction targets/durations, complete port binding, connected solids, and rigid-part interference over the named-motion scenario; run them faceted and retain the expected red evidence. Red: four failures for undeclared `input_angle`/instructions and rejected state binding; the pre-existing rigid connectivity baseline passed.
- [x] 4.2 Implement the motor/encoder envelopes, root driver, derived fixed/input/planet/output motion, `Home`, `One Motor Turn`, and `One Output Turn` instructions, and material-based colours until all root and scenario contracts pass faceted. Green: 5/5 root/scenario contracts before snapshot-preview coverage; the complete named path samples every 0.5 s.
- [x] 4.3 If upstream seated geometry overlaps, add `simulation/seats.py` with the exact pair-and-volume inventory and make both changed and newly introduced overlap fail without an epsilon. The 16-pair inventory spans measured exact/faceted volumes with pair-specific bounds and global epsilon 0. Centering the encoder PCB added a 59.739 mm³ cover collision; shifting the cross-roller outer race another 0.05 mm grew its accepted seat from 38.398 to 76.797 mm³. Both failed; restored root run green.
- [x] 4.4 Mutate the output-ratio sign and remove one root port binding, confirm the scenario/ratio and wiring contracts fail, restore both, and record any contract blind spot here. Negating the ratio returned -45° and failed the independent +45° relation; removing the output-stack binding stopped construction with `OutputStack.output_angle must be bound by its parent`. Both were restored. Visual symmetry remains unable to prove whole-tooth planet spin, so serialized operations and the independent kinematic contract remain essential.

## 5. Verification and project record

- [x] 5.1 Run the faceted full regression separately for every simulation node file, including the root, with no warning from a touched class. PASS: `parts.py` 0 contracts/build-only, `hardware.py` 0/build-only, `output_stack.py` 6/6, `reducer.py` 8/8 after time-preview coverage, `actuator.py` 6/6 after time-preview coverage; no touched-class warning.
- [x] 5.2 Run `solid build`; inspect `viewer.json` for the fixed/input/planet/output tree, symbolic operations, one driver, three instructions, piece inventory, and existence of every rigid model file. PASS: export v4, one driver, three instructions, two derived bindings, 17 piece records, three occurrences each for planet gear/bearing/pin, and all model paths present.
- [x] 5.3 Render and inspect home and driven snapshots in isometric and axial views with the default renderer, then remove the generated images. PASS: full-actuator isometric pair shows a coherent closed shell and 45° output advance; reducer axial pair exposes three 120° planets and their driven orbit. Generated PNGs removed after review.
- [x] 5.4 Extend `README.md` with exact build/test/snapshot commands, driver and instruction meanings, ground-up models, source/catalogue boundary, limitations, and measured findings.
- [x] 5.5 Run the exact full regression once for every node file, record the certified result and tested source fingerprint, and leave the default build green. PASS: `parts.py` 0/build-only, `hardware.py` 0/build-only, `output_stack.py` 6/6, `reducer.py` 8/8, and `actuator.py` 6/6 in 1283.68 s. Certified STEP SHA-256: `715965dc130555453d7d76d7ab51beeb8a5566a35cc37aae25cbb81151c879a3`.
- [x] 5.6 Fill `Purpose` in every capability spec, sync the delta specs, validate and archive `simulate-the-opentorque-actuator`, then commit the completed simulation record inside the OpenTorque repository. Three main specs synced with non-placeholder purposes; strict validation passed 4/4 items; archived to `openspec/changes/archive/2026-09-07-simulate-the-opentorque-actuator/`. The project implementation commit follows the archive operation.
